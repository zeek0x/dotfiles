{
    "name": "zeek0x/dotfiles image",
    "build": { "dockerfile": "Dockerfile" },
    "postCreateCommand": "./setup.sh",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/zsh"
    }
}
